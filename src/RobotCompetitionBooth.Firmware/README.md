# Robot Competition Booth Firmware

This directory is reserved for the robot's PlatformIO firmware project.

The conventional `include`, `lib`, `src`, and `test` directories are already present. A `platformio.ini` is intentionally not included because the target board and framework have not been selected yet.

Initialize the project after choosing the controller board:

```powershell
pio project init --board <board-id>
```
